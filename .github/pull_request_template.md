## What this changes

<!-- One or two sentences. Link the issue it closes, if there is one. -->

## Why

<!-- What was wrong or missing. -->

## Checklist

- [ ] `pnpm test:coverage` passes - the package is held at 100%
- [ ] `pnpm lint`, `pnpm typecheck` and `pnpm format:check` pass
- [ ] Behaviour that touches git or process spawning has a test that would fail without the change
- [ ] README updated if a flag or the API changed
